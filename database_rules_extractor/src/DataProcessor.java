//TO DO: Katerina
public class DataProcessor {
	/**
	 * Processes the data from the tables that it gets as input and creates maps for the 
	 * frequencies of the values in them. It also uses the thresholds for support and 
	 * confidence to generate additional maps for the tuples
	 */
}
